{
  "name": "translation-api",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node dev.js",
    "predev": "npm run schema:build",
    "test": "node --test test/",
    "pretest": "npm run schema:build",
    "eval:adversarial": "node --env-file=../.env ../evals/scripts/eval-adversarial.js",
    "schema:build": "tsc -p schema/tsconfig.json",
    "gcp-build": "npm run schema:build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@google-cloud/functions-framework": "^3.4.5",
    "@google/genai": "^1.50.1",
    "openai": "^4.85.0",
    "wanakana": "^5.3.1"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=20.12"
  }
}
