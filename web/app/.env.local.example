# Copy to .env.local (gitignored) to point the web app at a locally running
# API instead of the deployed Cloud API Gateway.
#
# Requires `cd api/src && npm run dev` running separately (serves on
# http://localhost:8080 by default).
VITE_API_URL=http://localhost:8080
