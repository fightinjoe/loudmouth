# Phrasebook Look-up UX — Progress Log

No tasks started yet. **Unblocked as of lookup-api LK-005 (eval harness) landing** — all of LK-001–005
pass, so `/lookup` is live and PH-002 onward can start immediately. PH-000 (legacy retirement) and
PH-001 (data model) have no dependencies and can start immediately in parallel; PH-002 depends on
PH-001 only, not on lookup-api anymore.
