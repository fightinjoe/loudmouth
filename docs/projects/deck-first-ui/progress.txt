## DU-007 — DONE (commit 4c289d7)
- renderTranslationArea(card, mode): comprehension→skeleton divs with data-translation attr; review→visible text; reverse→empty
- wireTranslationReveal(): mousedown/touchstart reveals, mouseup/mouseleave/touchend/touchcancel restores skeleton
- card-review-content wrapper div groups review-card + translation container
- CSS: .review-translation--skeleton, --visible, --revealed, skeleton-line styles
- 46 tests pass

## DU-006 — DONE
- openCardReview() added to deck-view.js; appended panel div to appEl
- Slide-up via translateY(100%)→0, 300ms ease; .card-review-panel--visible class
- renderReviewCardContent(): comprehension/review → text bold + reading; reverse → translation
- Card tap → speak(reading||text, lang); back button → slide out + remove from DOM
- Touch swipe (50px threshold) advances/retreats currentIndex, no wrap-around
- 46 tests pass; build clean

## DU-005 — DONE (commit 5c06181)
- Added `speak` import from tts.js in deck-view.js
- Delegated click on .deck-view-list; matches .card-row-play via closest()
- stopPropagation prevents row-body handler (DU-006) from firing
- speaks card.reading || card.text with deck.lang
- 46 tests pass

## DU-004 — DONE (commit 6549ca8)
- renderCardRow(card, mode) added to deck-view.js
- comprehension: text bold + reading caption; review: text · reading inline + translation caption; reverse: translation text only
- All three show play button on right
- 46 tests pass

## DU-003 — DONE (commit bec6581)
- Created app/src/styles/deck-view.css: #f5f5f0 bg, white card rows, 64px height, 6px gap, rounded corners
- Imported deck-view.css in main.js
- Empty state uses .deck-view-empty class; #deck-view-screen overrides .screen padding to 0
- deck-view-header: flex row, deck title btn + gear icon
- deck-view-list: flex col, gap 6px, scrollable
- All 46 tests pass; browser verified (empty state shows correct bg/text)

## Codebase Patterns
- DB is in app/src/db.js; tests use fake-indexeddb via createDb({ indexedDB, IDBKeyRange })
- All DB helpers accept an optional `store` param (defaults to singleton `db`) — always use this in tests
- Mode values as of DU-001: 'comprehension', 'reverse', 'review' (old: target-lang, reading, native-lang)
- Dexie version is now 3; add new versions at the end of the chain in createDb()

## DU-002 — DONE (commit b8acadb)
- router.js: single `deck` route via renderDeckView; legacy routes redirect to #deck
- deck-view.js stub: resolves deckId from params → localStorage → getRecentDecks(1)
- Exports getLastDeckId / setLastDeckId for DU-011
- localStorage key: loudmouth.lastDeckId
- All 46 tests pass

## DU-001 — DONE (commit 642d435)
- Added Dexie v3 with lastAccessedAt index on decks
- Migration: target-lang/reading → comprehension, native-lang → reverse
- Added updateDeckAccessTime(deckId, store) and getRecentDecks(n, store)
- Updated default mode for new decks/system decks to 'comprehension'
- All 46 tests pass
